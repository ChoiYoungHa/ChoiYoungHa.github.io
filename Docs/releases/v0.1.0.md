# v0.1.0 Web 3D 릴리스 초안

- 예정 태그: `v0.1.0-web3d` (**아직 생성하지 않음**)
- 초안 최초 작성 HEAD: `73fdaac`
- R35-B 재대조 HEAD: `6566b2d`
- R46-C M3 문서 반영 HEAD: `322be8e`
- production URL: **TBD(M4-20)**
- 최종 M4 성능: **TBD(M4-23A/M4-23B/M4-23C)**

## 태그 이력

| 태그 | 단계 |
|---|---|
| `v0.0.0-bootstrap` | M0-b 부트스트랩 |
| `v0.1.0-m1` | M1 지형·동선 |
| `v0.2.0-m2` | M2 거대 수목·마을, 조건부 PASS |
| `v0.1.0-web3d` | 최종 공유 릴리스 예정, 미생성 |

## M2 low 중앙값

실제 빌드, 1280×720 `low`, `?route=bench` 60초, 30초 워밍업 후 각 3회 중앙값이다.

| backend | 평균 fps | 하위 1% fps | 1초 끊김 | calls | programs | texture GPU MB | JS heap peak MB |
|---|---:|---:|---:|---:|---:|---:|---:|
| WebGPU | 125.22 | 33.31 | 0 | 63 | 40 | 36.88 | 193.07 |
| WebGL2 | 140.37 | 35.91 | 0 | 63 | 40 | 36.88 | 213.92 |

근거: [`m2-gate.md`](../decisions/m2-gate.md), [`m2-runs.csv`](../perf/m2-runs.csv). 이 값은 M2 기준이며 최종 성능을 뜻하지 않는다.

## M3 룩 결과

M3 룩 판정은 **L1~L5 5/5 PASS**다. 자동 명제는 S1=`vista-mid`와 S2=`vista-start`의 지정값을 사용하고, L4는 흑백 실루엣 수동 판정을 사용했다.

| 명제 | 채택값 | 판정 |
|---|---|---|
| L1 깊이별 채도 | S1 near 32.4%, far 11.4% | PASS |
| L2 온난→한랭 hue | S1 near 49.7°, far 212.2° | PASS |
| L3 원경이 밝음 | S1 near 67.3, far 134.1 | PASS |
| L4 랜드마크 실루엣 | 줄기/하늘 Δ37.2, 수관/하늘 Δ42.4; 흑백 수동 | PASS |
| L5 전역 채도 | S2 20.5% ≤ 22% | PASS |

파일 순서 1·2·3의 자동 판정은 before `1/4·2/4·1/4`에서 after `1/4·4/4·3/4`로 개선됐다. 채택값은 NeutralToneMapping, exposure `0.44`, `hueStrength=0.97`, `lumaGain=0.34`, `satFar=0.25`, `hazeMix=0.4`이며, 근거는 [`l1-l5-decision.json`](../lookdev/l1-l5-decision.json)과 [`m3-tonemap.md`](../lookdev/m3-tonemap.md)다.

### M3 잔여 관찰·미확정

- S3(`vista-village`) 원경/하늘 luma는 **166.4 > 목표 상한 145**다. 최종 L3는 지정 샷 S1의 134.1로 PASS했으므로 이 값은 샷별 잔여 편차다.
- L4의 세 번째 보조 체크인 줄기/수관 휘도차는 **5.1 < 자체 문턱 8**이며, 수관/줄기 폭 비율 7.15의 형태 구분으로 수동 PASS했다. master 재검토 가능한 정성 판정이다.
- 단발 성능에서 programs는 **40/40(측정/예산)**으로 여유가 없고, 1% low는 **19.9fps**로 20fps 관문 바로 아래다. 둘 다 R45-C의 동일 조건 3회 중앙값으로 재판정해야 한다.
- 따라서 M3 룩 5/5 PASS와 M3-GATE 완료는 동일하지 않으며, 성능·soak GATE는 **TBD(R45-C)**다.

## 에셋·라이선스

- 자산 대장: [`src/data/assets.csv`](../../src/data/assets.csv)
- Kenney Nature Kit 식생·바위: CC0, attribution 불필요.
- Poly Haven `Kloppenheim 03 Pure Sky` HDRI: 자산 ID `asset.env.sky.hdri.a`, CC0, attribution 불필요. R32-B에서 `assets.csv`에 19열 행을 추가했으며 런타임 경로는 `public/env/sky_1k.hdr`이다. 취득·선택 근거는 [`hdri-candidates.md`](../licenses/hdri-candidates.md)다.
- 거대 수목·집·지붕: 프로젝트 소유 절차적 자산, `assets.csv`에 생성 코드·tris·팔레트 근거 기록.

자산 대장 재대조 결과는 총 14행이며 Kenney 6행·Poly Haven 1행·절차적 프로젝트 자산 7행이다. 라이선스는 CC0 7행·Project-owned 7행이고, 모든 행은 19열이다.

## 미확정·릴리스 차단 항목

| 항목 | 상태 |
|---|---|
| 프로세스 총 RAM ≤24GB | 영하님 작업관리자 Chrome 프로세스 트리 수동 캡처 대기 |
| 외부 테스터 | 최소 3명 실제 세션 미수집 |
| M3 성능·soak GATE | **TBD(R45-C)**; programs 40/40·단발 1% low 19.9 재측정 대기 |
| production URL | **TBD(M4-20)** |
| 최종 M4 low 성능 | **TBD(M4-23A/M4-23B/M4-23C)** |
| 최종 태그 `v0.1.0-web3d` | 미생성; M4-26 완료 판정 때 생성 |

이 문서는 초안이며 URL·최종 성능·RAM·외부 판정이 채워질 때까지 릴리스 완료 문서가 아니다.
