# base tris 계약 결정 브리프

> 영하님 결정 1개: **A 품질 축소 / B 지형 축소 / C 원래 base 예산 복원** 중 하나를 선택합니다. master 권고는 **C**입니다.

## 1. 지금 확정된 사실

- `grassLite`만 켠 low worst는 **312,434 tris로 PASS**지만 근경 밀도·색감 GPU 룩 검증 중이다.
- `grassLite+rockLite`를 켠 base worst는 **675,234 tris**로 현재 자동검사의 600K 상한을 **75,234 초과**한다.
- base 675,234의 구성은 식생 **536,000(79.3799%)**, 지형 **131,072(19.4113%)**, 바위 **4,800(0.7109%)**, 길·마을·hero 합계 **3,362(0.4979%)**다.
- 문서 계약이 어긋나 있다. `계획서.md §4-1` 원표는 삼각형 예산을 **low ≤600K / base ≤1.1M**으로 나누지만, `로드맵.md M4-14`와 현재 자동검사는 **≤600K 단일값**을 쓴다.

근거: `Docs/perf/m4-scene-tris.json`, `Docs/perf/m4-tris-plan.md`, `계획서.md §3-6·§4-1`, `로드맵.md M4-14`.

## 2. 선택지 3개

아래 A·B 수치는 현재 base의 `grassLite+rockLite` 675,234를 출발점으로 한 생성기 합산값이다. 성능·룩 영향은 아직 GPU에서 실측하지 않았으므로 **추정**이다.

| 안 | 바꾸는 값 1개 | base tris | 예산 판정 | 룩 위험 | 성능 영향 예상 | 계획서 §8-1 후퇴 순서와의 일치 | 되돌리기 난이도 |
|---|---|---:|---|---|---|---|---|
| **A. 식생 수 축소** | `base.grassInstances.count` **20,000→15,000** | **541,234** | 600K PASS, 58,766 여유 | 중~높음: 초원 밀도·원경 덩어리감 감소 | 추정 개선: 식생 536K→402K, 인스턴스 25% 감소 | **3단계 콘텐츠 축소 방향과 일치**, 다만 표의 첫 값 6,000보다 완만한 중간값 | 쉬움: JSON 한 값 복원 |
| **B. 지형 세그먼트 축소** | `SEGMENTS_PER_CHUNK` **64→28** | **569,250** | 600K PASS, 30,750 여유 | 높음: 경사·실루엣·접지의 면 분할이 보일 수 있음 | 추정 개선: 지형 131,072→25,088 tris | **불일치**: 3단계는 지형 면적 250→160→100m이지 세그먼트 축소가 아님 | 쉬움: 상수 복원, 단 캡처 재검증 큼 |
| **C. 원계약 복원** | base tris limit **600K→1.1M** | **675,234** | §4-1 base 1.1M 기준 PASS, 424,766 여유 | 없음: 렌더 값 불변 | 없음: 기준만 바로잡으며 실제 부하는 그대로 | 후퇴가 아니라 **측정·계약 오류 제거(1단계) 전제 정리** | 쉬움: 검사기·로드맵의 base 한도 정정 |

### 600K의 근거와 fps 해석

`계획서.md §4-1`은 30fps 관문(33.3ms)에 25% 마진을 둔 low 설계 목표 25ms와 함께, 지형 131K·수목 120K·마을 32K·인스턴스를 합산해 **low 600K**를 계획값으로 잡았다. 같은 표의 base 열은 **1.1M**이다. low 단발 실측 `140.35 avg / 36.07 1% low fps`(`Docs/qa/m4-regress-hookup.csv`, 약 140/37 수준)는 low에 성능 여유가 있다는 신호지만, **900p·DPR 1.5·base 효과를 측정한 값이 아니므로 base 여유의 증명은 아니다**.

## 3. master 권고 — C, 원래 base 1.1M 계약 복원

1. 단일 원본 `계획서.md §4-1`이 이미 low와 base를 600K/1.1M으로 분리하므로 새 기준을 발명하지 않는다.
2. 제출 관문은 720p low이고 low grassLite가 312,434로 통과하므로, 잘못 확장된 low 상한 때문에 선택형 base 룩을 먼저 훼손하지 않는다.
3. 카파시식으로 **검사기의 base 한도 한 값만** 바로잡고 재측정한다. base 실제 성능이 목표를 못 넘을 때만 §8-1의 1→2→3 순서로 값 하나씩 후퇴한다.

## 4. 결정 후 실행 절차

1. **master** — C 승인 시 `Automation/check-budgets.mjs`와 `로드맵.md M4-14`에서 preset별 low 600K/base 1.1M 계약을 명시하고, 변경 계약이 한 종류인지 diff로 확인한다(A/B 선택 시 각각 `quality-presets.json` 또는 `Terrain.tsx` 한 값만 변경).
2. **worker-codex** — `Automation/scene-tris.mjs`로 low/base worst·typical을 재생성하고 `Docs/perf/m4-scene-tris.json`의 합계·예산 판정·source를 검증한다.
3. **worker-claude 또는 codex-A** — actual build에서 선택한 base를 900p·30초 워밍업·60초 동선 3회 측정하고 fps/1% low/programs와 고정 vista 룩을 기록해 keep/revert를 판정한다.
